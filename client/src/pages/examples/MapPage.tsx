import MapPage from "../MapPage";
import { ThemeProvider } from "../../components/ThemeProvider";

export default function MapPageExample() {
  return (
    <ThemeProvider>
      <MapPage />
    </ThemeProvider>
  );
}
